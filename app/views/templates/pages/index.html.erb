<%# ---
name: Homepage
fields: {}
regions:
  - main
features:
  - content_block
  - header
  - image
--- %>

<% content_for :jumbotron do %>
  <%= render 'public/partials/jumbotron' %>
<% end %>

<div class="lead text-center"><%= markdown current_object.body %></div>

<%= render_feature(:header) %>

<div class="masonry">
  <%= render_feature(:image) %>
</div>

<div class="hours text-center border-top-dotted">
  <%= render_feature(:content_block) %>
</div>
